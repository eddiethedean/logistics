[behave]
# Define the features directory
features = features

# Define the steps directory
steps = features/steps

# Set the default format for output
format = pretty

# Set the default color output
color = true

# Set the default logging level
logging_level = INFO

# Set the default timeout for steps
timeout = 30
